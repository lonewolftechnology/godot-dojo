<?xml version="1.0" encoding="UTF-8" ?>
<class name="DojoCallback" inherits="RefCounted" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		A class for handling callbacks from Dojo subscriptions.
	</brief_description>
	<description>
		The DojoCallback class provides a mechanism to handle updates and errors from Dojo subscriptions, such as entity updates or event notifications.
	</description>
	<tutorials>
	</tutorials>
	<members>
		<member name="on_error" type="Callable" setter="set_on_error" getter="get_on_error" default="Callable()">
			The callable to be executed when an error occurs.
		</member>
		<member name="on_update" type="Callable" setter="set_on_update" getter="get_on_update" default="Callable()">
			The callable to be executed when an update is received.
			[codeblock]
			func callback(data:Dictionary, type:String):
				if type == "Entity":
					var result = data["models"]
					add_entry_to_output("%s CALLBACK" % type, result)
				if type == "Message":
					var _data = data["data"]
					for _key in _data:
						printt("**",_key)

			var _dojo_callback:DojoCallback = DojoCallback.new()
			_dojo_callback.on_update = callback.bind("Entity")
			[/codeblock]
		</member>
	</members>
</class>
